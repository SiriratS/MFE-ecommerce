import { Route } from '@angular/router';
import { ProfileComponent } from '@ecommerce-platform/profile';

export const PROFILE_ROUTES: Route[] = [
    {
        path: '',
        component: ProfileComponent,
    },
];
